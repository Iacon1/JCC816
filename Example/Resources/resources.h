// Resource files for the game

#include <stdint.h>

const extern uint8_t tilemap[2048];
const extern uint8_t tile[64*32];
const extern uint8_t palette[32];

const extern uint8_t audioDriver1[];
const extern uint8_t audioDriver2[];
const extern uint8_t music[];
